[project]
name = "image-segmentation-unet"
version = "0.1.0"
requires-python = "~=3.12"
dependencies = [
    "torch>=2.4,<2.5",
    "torchvision>=0.19,<0.20",
    "numpy>=1.24",
    "pillow>=9.0",
    "tqdm>=4.0",
    "matplotlib>=3.10",
    "torch-directml>=0.2"
]

[tool.uv]
prerelease = "allow"
